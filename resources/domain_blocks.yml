- action:
    type: block
  trigger:
    url-filter: atwola\.com
- action:
    type: block
  trigger:
    url-filter: adinfo\.
- action:
    type: block
  trigger:
    url-filter: '[^a-zA-Z]ads.*?\.js'
